Licensed under the WTFPL, grab your copy at http://wtfpl.net/

Requirements:

* Python 3.5+

Installation Instructions:

* Run <code>pip install irc</code>
* Copy config.yml.template to config.yml
* Edit config.yml with your twitch credentials
